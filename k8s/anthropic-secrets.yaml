apiVersion: v1
kind: Secret
metadata:
  name: anthropic-secrets
type: Opaque
stringData:
  # Replace with your actual Anthropic API key
  # Or use: kubectl create secret generic anthropic-secrets --from-literal=api-key=$ANTHROPIC_API_KEY
  api-key: your-anthropic-api-key-here
